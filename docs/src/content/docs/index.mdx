---
title: Overview
description: Netstamp is split into a static docs and marketing site, a React service app, a Go API, and a shared React UI package.
icon: compass
head:
  - tag: title
    content: Overview | Netstamp Docs
---

## Workspace Map

The repository keeps the public site and product app as separate surfaces. `docs/` owns the public homepage and documentation. `web/` owns the authenticated React service app. `packages/ui/` contains reusable React primitives shared by both surfaces.

- [Start local](/docs/guides/getting-started/): Install dependencies and boot each workspace target.
- [Understand flow](/docs/reference/architecture/): See how the browser app, API, database, and docs relate.
- [Try the API](/openapi/): Browse generated OpenAPI and send requests from the docs.

## Where To Go Next

Use the left navigation to move across documentation sections. Use the search box to jump by title, keyword, or short description. Use the right table of contents to move within the current page.

:::tip This docs shell implements a top nav, sidebar, page TOC, search, callouts, code highlighting, and edit links directly in Astro instead of depending on Astro Starlight. :::

## Local Commands

These are the common commands from the root workspace.

```bash
pnpm install
just docs-dev
just web-dev
just backend-test
```
