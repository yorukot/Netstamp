<section class="hero" aria-labelledby="homepage-title">
	<canvas id="ns-canvas" aria-hidden="true"></canvas>
	<canvas id="ns-cable-canvas" aria-hidden="true"></canvas>

	<div class="vignette" aria-hidden="true"></div>
	<div class="hero-content">
		<h1 id="homepage-title">
			<span class="line1">Trace every path.</span>
			<span class="line2">Before it breaks.</span>
		</h1>
		<p class="sub">Open-source network observability from probes you control.</p>
		<div class="cta-row">
			<a href="/docs/guides/getting-started/" class="btn btn-primary">Get Started</a>
			<a href="https://github.com/yorukot/netstamp" class="btn btn-outline" target="_blank" rel="noreferrer">View on GitHub</a>
		</div>
	</div>
	<div class="hero-horizon" aria-hidden="true"></div>
</section>

<style>
	.hero {
		position: relative;
		display: grid;
		place-items: center;
		min-height: 108svh;
		padding: 8rem 1rem 18svh;
		overflow: hidden;
		isolation: isolate;
		color: #ffffff;
		background: #0a0a0f;
	}

	#ns-canvas,
	#ns-cable-canvas {
		position: absolute;
		inset: 0;
		display: block;
		width: 100%;
		height: 100%;
		pointer-events: none;
	}

	#ns-canvas {
		z-index: 0;
	}

	#ns-cable-canvas {
		z-index: 1;
	}

	.vignette {
		position: absolute;
		z-index: 2;
		inset: 0;
		background:
			linear-gradient(180deg, rgba(0, 0, 0, 0.8), transparent 28%, transparent 70%, rgba(234, 106, 26, 0.14)),
			radial-gradient(ellipse 58% 48% at 50% 50%, rgba(10, 10, 15, 0.88), rgba(10, 10, 15, 0.4) 54%, transparent 100%);
	}

	.hero-horizon {
		position: absolute;
		z-index: 2;
		right: -8vw;
		bottom: -6svh;
		left: -8vw;
		height: 22svh;
		background: var(--home-white);
		transform: rotate(-2deg);
		transform-origin: center;
	}

	.hero-content {
		position: relative;
		z-index: 4;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1.5rem;
		padding: 2rem;
		text-align: center;
		pointer-events: auto;
	}

	h1 {
		display: flex;
		flex-direction: column;
		gap: 0.05em;
		margin: 0;
		color: #ffffff;
		font-family: "TASAExplorer", "TASA Explorer", "Inter", system-ui, sans-serif;
		font-size: clamp(3rem, 7.2vw, 7.6rem);
		font-weight: 200;
		letter-spacing: -0.03em;
		line-height: 1;
	}

	.line1,
	.line2 {
		display: block;
	}

	.line2 {
		color: rgba(255, 255, 255, 0.88);
	}

	.sub {
		max-width: 540px;
		margin: 0;
		color: rgba(255, 255, 255, 0.5);
		font-family: "TASAOrbiter", "TASA Orbiter", "Inter", system-ui, sans-serif;
		font-size: 1rem;
		font-weight: 400;
		letter-spacing: 0;
		line-height: 1.55;
	}

	.hero :global(.cta-row) {
		margin-top: 0.5rem;
	}

	@media (min-width: 48rem) {
		.sub {
			font-size: 1.15rem;
		}
	}

	@media (max-width: 38rem) {
		.hero {
			min-height: 102svh;
			padding: 7rem 1rem 14svh;
		}

		.hero-content {
			width: 100%;
			padding: 1rem;
		}

		h1 {
			font-size: 3.2rem;
		}
	}
</style>
